# Schema ASU (Apex Standard Unified)

> Documentação a ser preenchida na **Reunião 2**, quando o schema JSON canônico
> for definido e os parsers Semgrep → ASU e Trivy → ASU forem implementados.

Campos previstos: `id`, `source_tool`, `repository`, `file_path`, `line_number`,
`severity`, `title`, `description`, `cve_id` (opcional), `timestamp`, `raw_output`.
